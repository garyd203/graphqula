"""Out-of-the-box bindings for various HTTP server frameworks.

Other bindings are possible, these are provided for convenience.

Note that each binding needs to be in an indpendent module that is not imported by another module,
so that we don't introduce unnecessary third party dependencies.
"""
