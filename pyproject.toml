[tool.poetry]
name = "gryphter"
version = "0.1.0"
description = "Server-side GraphQL for Pythonistas"
authors = ["garyd <github@gazza.id.au>"]
license = "LGPL"

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^1.9.1"

[tool.poetry.dev-dependencies]
black = "=22.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
